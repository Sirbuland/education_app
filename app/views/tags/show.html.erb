<h1 align="center"> <%= @category.name.capitalize%></h1>
<hr>

<div class = "well col-md-4 col-md-offset-2 ">
	<h4 class =" center description"><%= simple_format(@category.description)%></h4>
    <hr>
	 <div class = "pull-right">Created <%= time_ago_in_words(@category.created_at.strftime("%d/%m/%Y on %I:%M:%S"))%> ago.</div></p>
     <div class="category-actions">
		<%= link_to "Edit this category", edit_category_path(@category), class:"btn btn-xs btn-warning"%>  
		<%= link_to "Delete this category", category_path(@category), method: :delete ,
                 data: {confirm: "Are you sure you want to delete this category"},class:"btn btn-xs btn-danger"%>
		<%= link_to "Return to categories listing", categories_path(),class:"btn btn-xs btn-primary"%> 
        <%end%>
  
     </div>	
</div> 
	
