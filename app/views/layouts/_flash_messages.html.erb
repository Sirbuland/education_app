<% flash.each do |key, value| %>
	<% key = key.to_s.gsub('alert', 'danger').gsub('notice', 'info').gsub('error', 'danger') %>
  <div class="alert alert-<%= key %>">
  	<button type="button" class="close" data-dismiss="alert">&times;</button>
  	<%= value %>
  </div>
<% end %>
