<h1 align="center"> <%= @tag.name.capitalize%></h1>
<hr>

<div class = "well col-md-4 col-md-offset-2 ">
	<h4 class =" center description"><%= simple_format(@tag.description)%></h4>
    <hr>
	 <div class = "pull-right">Created <%= time_ago_in_words(@tag.created_at.strftime("%d/%m/%Y on %I:%M:%S"))%> ago.</div></p>
     <div class="category-actions">
		<%= link_to "Edit this tag", edit_tag_path(@tag), class:"btn btn-xs btn-warning"%>  
		<%= link_to "Delete this tag", tag_path(@tag), method: :delete ,
                 data: {confirm: "Are you sure you want to delete this tag"},class:"btn btn-xs btn-danger"%>
		<%= link_to "Return to tags listing", tags_path(),class:"btn btn-xs btn-primary"%> 
        <%end%>
  
     </div>	
</div> 
	
